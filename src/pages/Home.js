import React from 'react'

const uiType = ["button", "slider", "carousel", "loader"];
const items = [
  {
    id: 0,
    type: "slider",
    inspiredBy: "10x10",
    desc: "설명"
  }
];

const Home = ()=>{
    return(
        <div>
            <h2>홈</h2>
        </div>
    )
}

export default Home;